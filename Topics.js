/**
 * Hoisting - means func can be used even before they are declared
 * Call stack
 * Scope Chain
 * Call, Apply, Bind - 'this' in eventhandler always points to the element on which that eventhandler is attached
 * IIFE, Closures
 * Map, Filter, Reduce
 * Event propagation
 * Event delegation
 * Prototypes
 * Prototypal Inheritance, Prototype chain
 * Local storage
 * Convert JSON to String
 * Promises
 * Async Await
 * Promise Combinators: all, race, allSetted, any
 */

/**
 * Section 13: Bankist app
 * Section 15: Mapty app
 */

/**ES6 features from 2015
 * Promises
 * Exporting & Importing
 */

/**ES2017 fetures
 * Async await
 */

/**ES2020
 * Promise.allSettled()
 */

/**ES2021
 * Promise.any()
 */

/**ES2022
 * At method in Array
 */

/**
 * npm init
 * npm install leaflet "or" npm i lodash-es eg: npm i parcel@1
 * npx parcel index.html or npm run start ("start": " parcel index.html" is added in package.json)
 */

/**
 * git init
 * git config --global user.name USERNAME
 * git config --global user.email EMAIL
 * git config --global --list ==>will show the current user name & email set to
 * git status
 * git add fileName (or) git add -A =>adding all file/s to staging
 * git commit -m "message"
 * git push origin master  ==>will push changes to remote repo on github to master branch
 * git branch new-feature  ==>will create a branch
 * git chekout new-feature  ==>to switch to new branch
 *
 * git reset --hard HEAD ==>to reset all the files to previous commit BEFORE new changes are committed
 * git log ==>will list the log of commits& to get ID of a commit, then press "q" or sometimes ":q"to quit from log
 * git reset --hard IDofCOMMIT ==>to reset all the files to previous commit AFTER new changes are committed
 * git merge new-feature =>to merge current branch to new-feature branch
 * git remote add origin .gitURL  ==>will link local repo to the remote repository
 * echo "text content" >> start.txt  ==>will create text file & add content in it
 * cat start.txt  ==>will display content of the file
 */
